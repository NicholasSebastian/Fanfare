import React from 'react';
import { View, Text } from 'react-native';

export default function Summons() {
    return (
        <View>
            <Text style={{color: '#fff'}}>Summon Screen</Text>
        </View>
    );
}