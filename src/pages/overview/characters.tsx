import React from 'react';
import { View, Text } from 'react-native';

export default function Characters() {
    return (
        <View>
            <Text style={{color: '#fff'}}>Character Screen</Text>
        </View>
    );
}