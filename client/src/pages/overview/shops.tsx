import React from 'react';
import { View, Text } from 'react-native';

export default function Shops() {
    return (
        <View>
            <Text style={{color: '#fff'}}>Shops Screen</Text>
        </View>
    );
}