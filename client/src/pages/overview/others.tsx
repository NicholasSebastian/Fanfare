import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

export default function Others() {
    return (
        <View>
            <Text style={{color: '#fff'}}>Others page</Text>
        </View>
    );
}

const styles = StyleSheet.create({

});